export interface Options {
  gravity: boolean;
  parallax: boolean;
  accelerationLines: boolean;
}

export const defaultOptions: Options = {
  gravity: true,
  parallax: true,
  accelerationLines: false,
};
