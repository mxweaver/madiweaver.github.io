import React from 'react'

const styles = {
  container: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    height: '100vh'
  }
}

export default function Home() {
  return (
    <div style={styles.container}>
      <div>Maya Vera (home)</div>
    </div>
  )
}
